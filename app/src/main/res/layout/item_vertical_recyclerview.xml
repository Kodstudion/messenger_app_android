<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="150dp">

    <ImageView
        android:id="@+id/message_picture"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_marginLeft="10dp"
        android:background="@drawable/profile_picture"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

    </ImageView>

    <TextView
        android:id="@+id/message_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="20dp"
        android:textSize="19sp"
        android:gravity="center"
        android:text="Displayname"
        android:textColor="@color/black"
        app:layout_constraintBottom_toBottomOf="@+id/message_picture"
        app:layout_constraintStart_toEndOf="@+id/message_picture"
        app:layout_constraintTop_toTopOf="parent">

    </TextView>

    <TextView
        android:id="@+id/message_recent_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="20dp"
        android:layout_marginTop="30dp"
        android:gravity="center"
        android:text="RecentMessage"
        android:textColor="@color/black"
        app:layout_constraintStart_toEndOf="@+id/message_picture"
        app:layout_constraintTop_toTopOf="@+id/message_title">

    </TextView>

</androidx.constraintlayout.widget.ConstraintLayout>